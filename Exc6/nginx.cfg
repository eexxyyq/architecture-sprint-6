http {
   # Настройка зоны для ограничения запросов (не более 10 запросов в минуту на IP)
   limit_req_zone $binary_remote_addr zone=req_limit_per_ip:10m rate=10r/m;

   # Настройка upstream для балансировки нагрузки
   upstream backend_servers {
       server backend1.example.com;
       server backend2.example.com;
       server backend3.example.com;
   }

   server {
       listen 80;

       # Применяем лимит запросов для всей локации
       location / {
           limit_req zone=req_limit_per_ip burst=5 nodelay;
           proxy_pass http://backend_servers;
       }

       # Ошибка 429 для клиентов, превышающих лимит
       error_page 429 /429.html;
       location = /429.html {
           root /usr/share/nginx/html;
           internal;
       }
   }
}
